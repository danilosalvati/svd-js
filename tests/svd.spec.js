import {assert} from 'chai'

import {SVD} from '../src/index'

describe('SVD tests', () => {
  it('Sample Test', (done) => {
    assert.throws(() => SVD(), Error, 'Not implemented yet')
    done()
  })

})
